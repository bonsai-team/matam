#ifndef SWISSKNIFE_H
#define SWISSKNIFE_H

#include <stdlib.h>
#include <stdint.h>
#include <iterator>
#include <algorithm>
#include <vector>

template<typename T>
std::vector<T> intersection(std::vector<T> &, std::vector<T> &);

#endif // SWISSKNIFE_H